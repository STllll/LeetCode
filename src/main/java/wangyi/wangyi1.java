package wangyi;

import java.util.Scanner;

public class wangyi1 {
    public static void main(String[] args) {
      /*  Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int m = sc.nextInt();
        int[] game = new int[m];
        int[] play = new int[n];
        for(int i = 0; i < m; i++) {
            game[i] = sc.nextInt();
        }
        int flag = 0; //判断什么时候要消去
        int point = 0; //分数
        for(int i = 0; i < m; i++) {
            play[game[i] - 1]++;
            if(play[game[i] - 1] == 1) flag++;
            if(flag == n) {
                point++;
                for(int j = 0; j < n; j++) {
                    play[j]--;
                    if(play[j] == 0) flag--;
                }
            }
        }
        System.out.println(point);*/
        String str=null;
        str.concat("abc");
        str.concat("123");
        System.out.println(str);
    }
}
